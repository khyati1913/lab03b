#include <stdio.h>

int main() {
    int limit, num, i, j, k, l, count;

    printf("Enter upper limit to search Ramanujan numbers: ");
    scanf("%d", &limit);

    for (num = 1; num <= limit; num++) {
        count = 0;
        for (i = 1; i*i*i <= num; i++) {
            for (j = i; j*j*j <= num; j++) {
                if (i*i*i + j*j*j > num) break;
                for (k = i; k*k*k <= num; k++) {
                    for (l = k; l*l*l <= num; l++) {
                        if (k*k*k + l*l*l > num) break;
                        if (i*i*i + j*j*j == k*k*k + l*l*l && i != k && j != l) {
                            count++;
                        }
                    }
                }
            }
        }
        if (count >= 1) {
            // count >= 1 means at least two distinct pairs => a Ramanujan number
            printf("%d\n", num);
        }
    }

    return 0;
}

