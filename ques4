#include <stdio.h>

int main() {
    double pop = 100000.0;
    int year;

    for (year = 1; year <= 10; year++) {
        pop = pop * 1.10;   // increase by 10%
        printf("End of year %d: %.0f\n", year, pop);
    }

    return 0;
}
